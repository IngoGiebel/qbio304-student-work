setwd("//wsl.localhost/Ubuntu-22.04/home/uranus/QBio304 - Applied Bioinformatics/student-work/scripts")
print(environment())
setwd("//wsl.localhost/Ubuntu-22.04/home/uranus/QBio304 - Applied Bioinformatics/student-work/data")
this.path::here()
install.packages("this.path")
library(this.path)
this.path::here()
paste(this.path::here(), "/", "..", "data")
"werwer" + "sef"
this.path::this.path()
this.path::here()
paste0(this.path::here(), "/", "..", "data")
# Set the project's data subdirectory as the current working directory
setwd(paste0(this.path::here(), "/../data"))
getwd()
# Provides static code analysis for R. It checks for adherence to a given style,
# identifying syntax errors and possible semantic issues
library(lintr)
# For setting the current working directory relative to the filepath
# of this script
library(this.path)
# Tools for Data Copy-Pasta. RStudio addins and R functions that make
# copy-pasting vectors and tables to text painless.
library(datapasta)
# Hadley Wickham's collection of R packages for data science
library(tidyverse)
# Set the project's data subdirectory as the current working directory
setwd(paste0(this.path::here(), "/../data"))
# Read in the study design file
targets <- readr::read_tsv(
"studydesign-PRJCA004229.tsv",
col_types = "icccccfffcficfc")
View(targets)
head targets
head(targets)
View(targets)
# Read in the studydesign file
studydesign <- readr::read_tsv(
"studydesign-PRJCA004229.tsv",
col_types = "icccccfffcficfc")
# Read in the studydesign file
studydesign <- readr::read_tsv(
"studydesign-PRJCA004229.tsv",
col_types = "icccccfffcficfc")
str(studydesign)
typeof(studydesign)
class(studydesign)
# Read in the studydesign file
studydesign_df <- readr::read_tsv(
"studydesign-PRJCA004229.tsv",
col_types = "icccccfffcficfc")
# File paths to the Kallisto abundance.tsv files
abundance_filepaths <- file.path(
studydesign_df$`Run accession`,
"abundance.tsv")
all(file.exists(abundance_filepaths))
